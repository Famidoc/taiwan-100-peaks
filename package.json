{
  "name": "taiwan-100-peaks",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:data": "node scripts/build-data.js",
    "predeploy": "npm run build",
    "deploy": "node scripts/deploy.js"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^8.1.1"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "sharp": "^0.35.3",
    "xlsx": "^0.18.5"
  }
}
